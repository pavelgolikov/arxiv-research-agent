# Generated Results

- `reviews/` contains Markdown literature reviews produced during development.
- `checkpoints/` contains the corresponding serialized workflow state.
- `parsed/` contains manually saved extracted paper text used while testing.

These files are historical run artifacts; the application still uses its original
CLI defaults and output behavior.
